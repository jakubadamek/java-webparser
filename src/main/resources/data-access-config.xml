<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx-2.5.xsd">

	<!-- Instructs Spring to perfrom declarative transaction management on annotated classes -->
	<tx:annotation-driven />

	<!-- Deploys a in-memory "booking" datasource populated -->
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="org.hsqldb.jdbcDriver" />
		<property name="url" value="jdbc:hsqldb:mem:trickBenchmark" />
		<property name="username" value="sa" />
		<property name="password" value="" />
	</bean>

    <!-- ========================= TRANSACTIONS ========================= -->
    <!-- enable the configuration of transactional behavior based on annotations, used in Jdbc*Service -->
    <tx:annotation-driven transaction-manager="txManagerAnnotations"/>
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    
    <bean id="priceService" class="com.jakubadamek.robotemil.services.JdbcPriceService">
        <property name="dataSource" ref="dataSource"/>
    </bean>              	
    
    <bean id="settingsService" class="com.jakubadamek.robotemil.services.JdbcSettingsService">
        <property name="dataSource" ref="dataSource"/>
    </bean>
</beans>